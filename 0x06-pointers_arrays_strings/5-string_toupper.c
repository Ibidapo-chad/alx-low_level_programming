#include "holberton.h"

/**
 * program changes lowercase letters to uppercase letters
 * 
 */

char *string_toupper(char *)
{
  char *p = *;
  int i;
  for (i = 0; *(p + i) != 0; i++)
  {
    if ()
  }
}
